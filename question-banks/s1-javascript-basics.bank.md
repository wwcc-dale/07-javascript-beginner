---
bank_name: "Session 1: JavaScript Fundamentals"
---

1. Which keyword should you use when the value of a variable will NOT change?
a) var
*b) const
c) let
d) static

1. Which data type is best for storing a student's name?
a) Number
*b) String
c) Boolean
d) Array

1. Which data type is best for storing whether homework is finished?
a) Number
b) String
*c) Boolean
d) Object

1. What does this code print?
```js
let score = 85;
if (score >= 90) {
  console.log("A");
} else {
  console.log("Keep going");
}
```
a) A
*b) Keep going
c) 85
d) Error

1. Which operator checks if two values are exactly equal in JavaScript?
a) =
b) ==
*c) ===
d) !=

1. Which line correctly creates a variable that might change later?
a) const total = 0;
*b) let total = 0;
c) var total == 0;
d) number total = 0;

1. What will this code do?
```js
const passingScore = 70;
let myScore = 72;
if (myScore >= passingScore) {
  console.log("You passed!");
} else {
  console.log("Try again.");
}
```
*a) Print "You passed!"
b) Print "Try again."
c) Print 72
d) Cause an error

1. True or false: You can change the value stored in a variable declared with `const`.
*a) False
b) True

1. In this code, what is the value of `isHigh`?
```js
let temperature = 90;
let isHigh = (temperature > 80);
```
*a) true
b) false
c) 90
d) "90"

1. What does `console.log` do?
a) Saves data to a database
*b) Shows output in the console
c) Starts the program
d) Clears the screen

1. Which operator would you use to check if `age` is at least 18?
a) age =>
*b) age >= 18
c) age =>= 18
d) age =< 18

1. What is wrong with this code?
```js
const studentName = "Jordan";
studentName = "Alex";
```
a) Strings must use single quotes
*b) You cannot change a `const` variable
c) Variable names cannot use capital letters
d) Nothing is wrong

1. Which of these is a valid variable name in JavaScript?
a) 1stScore
b) total-score
*c) totalScore
d) total score

1. What will this code print?
```js
let itemsInCart = 0;
if (itemsInCart > 0) {
  console.log("You have items in your cart.");
} else {
  console.log("Your cart is empty.");
}
```
*a) Your cart is empty.
b) You have items in your cart.
c) 0
d) Error

1. How would you check if `status` is NOT equal to `"complete"`?
a) if (status = "complete")
*b) if (status !== "complete")
c) if (status <> "complete")
d) if (!status "complete")

1. Which statement is true?
a) `let` and `const` mean the same thing.
*b) Use `let` when the value will change, `const` when it should not.
c) You should never use `const`.
d) You must use `var` in modern JavaScript.
